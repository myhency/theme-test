import React from 'react';
import CountingCard from '../components/CountingCard';

const SiteCountingView = () => (
    <CountingCard title={'Sites'} type='info' count={1} />
);

export default SiteCountingView;